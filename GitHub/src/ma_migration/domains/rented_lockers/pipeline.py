from __future__ import annotations
import pandas as pd

def prepare_rented_lockers(locwea_spare: pd.DataFrame) -> pd.DataFrame:
    """Notebook section is short; keep as a pass-through hook for future.
    Provide here as a domain placeholder for reuse.
    """
    return locwea_spare.copy()
